# This is a Databricks asset bundle definition for test.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test
  uuid: c3a95568-da6b-428b-8933-53a0827a2c00

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c4cff0e8-cbbc.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-c4cff0e8-cbbc.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/harshsaxena400@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/harshsaxena400@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: harshsaxena400@gmail.com
        level: CAN_MANAGE
